fn main() {
    println!("Jai Balayya!");
}
// to run this first build the project using `cargo build` and then run it using `cargo run` or ./target/debug/day_1
//cargo check is much faster than cargo build, because it skips the step of producing an executable.